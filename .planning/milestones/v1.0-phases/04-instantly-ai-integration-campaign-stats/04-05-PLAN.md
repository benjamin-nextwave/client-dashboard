---
phase: 04-instantly-ai-integration-campaign-stats
plan: 05
type: execute
wave: 4
depends_on: ["04-04"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "Dashboard builds and renders without runtime errors"
    - "All stat cards, charts, and modal display correctly with Dutch labels"
    - "Brand color is applied consistently across all components"
---

<objective>
Visual verification of the complete Overzicht dashboard.

Purpose: Confirm that all Phase 4 UI components render correctly, charts display properly, the contact list modal works, and brand theming is consistent.

Output: Human-verified dashboard ready for production.
</objective>

<execution_context>
@C:\Users\bstei\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\bstei\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-instantly-ai-integration-campaign-stats/04-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build verification and seed data check</name>
  <files></files>
  <action>
Run `npm run build` to verify the complete application builds without errors. If there are type errors or build failures, fix them before proceeding to human verification.

Check if there is existing seed data for synced_leads and campaign_analytics tables. If not, the dashboard will show empty/zero states -- that is acceptable for verification (the empty states should display gracefully with "Geen data beschikbaar" messages rather than errors).

If build succeeds and the dev server starts cleanly, proceed to the checkpoint.
  </action>
  <verify>Run `npm run build` -- must complete without errors.</verify>
  <done>Application builds cleanly with all Phase 4 components.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete Overzicht dashboard with stats cards (Reactie vereist alert, monthly KPIs, Contacten in database), contact status chart, industry/job title pie charts, ICP patterns section, and contact list modal. All in Dutch with brand color theming.</what-built>
  <how-to-verify>
    1. Start dev server: `npm run dev`
    2. Log in as a client user at http://localhost:3000/login
    3. Navigate to the Overzicht page (should be the default dashboard page)
    4. Verify the following:
       a. Page title says "Overzicht"
       b. If unanswered positive count > 0, alert banner shows in brand color with "reactie(s) vereist"
       c. Four stat cards are displayed: "Totaal aantal reacties", "Geleverde leads", "Contacten in database", "Verzonden mails"
       d. "Contacten in database" card has a "Bekijk lijst" button
       e. Click "Bekijk lijst" -- modal opens with contact table (may be empty if no synced data)
       f. Modal can be closed with X button or ESC key
       g. Contact status chart section visible (may show "Geen data beschikbaar" if empty)
       h. Industry and job title pie chart sections visible
       i. ICP Vorming section with positive lead patterns visible
       j. Brand color is applied to alert banner and card accents
    5. Check browser console for any JavaScript errors
    6. Resize browser to mobile width -- verify cards stack properly
  </how-to-verify>
  <resume-signal>Type "approved" if dashboard looks correct, or describe any issues to fix.</resume-signal>
</task>

</tasks>

<verification>
- Build passes without errors
- Dashboard renders in browser without console errors
- All Dutch labels present and correct
- Brand color applied consistently
- Modal opens/closes properly
- Empty states handled gracefully
</verification>

<success_criteria>
Human confirms the Overzicht dashboard renders correctly with all components, proper Dutch labels, brand theming, and graceful empty states.
</success_criteria>

<output>
After completion, create `.planning/phases/04-instantly-ai-integration-campaign-stats/04-05-SUMMARY.md`
</output>
