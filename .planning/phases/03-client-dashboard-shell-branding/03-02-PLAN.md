---
phase: 03-client-dashboard-shell-branding
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/app/(client)/dashboard/page.tsx
  - src/app/(client)/dashboard/inbox/page.tsx
  - src/app/(client)/dashboard/verzonden/page.tsx
  - src/app/(client)/dashboard/voorkeuren/page.tsx
  - src/app/(client)/dashboard/dnc/page.tsx
  - src/app/(client)/dashboard/afspraken/page.tsx
autonomous: false

must_haves:
  truths:
    - "Dashboard layout includes working navigation to all 6 feature pages"
    - "Each navigation link routes to a page that renders without error"
    - "Active navigation state updates correctly when navigating between pages"
    - "All page titles and placeholder text are in Dutch"
    - "Brand color is visible in sidebar and active nav state"
  artifacts:
    - path: "src/app/(client)/dashboard/inbox/page.tsx"
      provides: "Inbox placeholder page"
      contains: "Inbox"
    - path: "src/app/(client)/dashboard/verzonden/page.tsx"
      provides: "Verzonden placeholder page"
      contains: "Verzonden"
    - path: "src/app/(client)/dashboard/voorkeuren/page.tsx"
      provides: "Voorkeuren placeholder page"
      contains: "Voorkeuren"
    - path: "src/app/(client)/dashboard/dnc/page.tsx"
      provides: "DNC placeholder page"
      contains: "DNC"
    - path: "src/app/(client)/dashboard/afspraken/page.tsx"
      provides: "Afspraken placeholder page"
      contains: "Afspraken"
  key_links:
    - from: "src/components/client/sidebar-nav.tsx"
      to: "src/app/(client)/dashboard/*/page.tsx"
      via: "Next.js Link href matching route paths"
      pattern: "/dashboard/"
---

<objective>
Create placeholder pages for all 6 client dashboard routes and verify the complete branded shell works end-to-end.

Purpose: Complete the navigation by adding routable pages for all sidebar links, then verify branding, navigation, and layout work correctly with a real client login.
Output: 6 working client dashboard routes with Dutch placeholder content, plus visual verification of the branded shell.
</objective>

<execution_context>
@C:\Users\bstei\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\bstei\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-client-dashboard-shell-branding/03-01-SUMMARY.md

@src/app/(client)/layout.tsx
@src/app/(client)/dashboard/page.tsx
@src/components/client/sidebar-nav.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create placeholder pages and update Overzicht</name>
  <files>
    src/app/(client)/dashboard/page.tsx
    src/app/(client)/dashboard/inbox/page.tsx
    src/app/(client)/dashboard/verzonden/page.tsx
    src/app/(client)/dashboard/voorkeuren/page.tsx
    src/app/(client)/dashboard/dnc/page.tsx
    src/app/(client)/dashboard/afspraken/page.tsx
  </files>
  <action>
    1. **Update `src/app/(client)/dashboard/page.tsx`** (Overzicht):
       - Keep as server component
       - Update to show a proper page header: `<h1 className="text-2xl font-bold text-gray-900">Overzicht</h1>`
       - Add placeholder content: `<p className="mt-2 text-gray-600">Uw campagnestatistieken worden hier weergegeven.</p>`
       - Add a branded accent element to visually confirm theming works: `<div className="mt-6 rounded-lg border-l-4 border-brand bg-brand/5 p-4"><p className="text-sm text-gray-700">Dashboard wordt binnenkort aangevuld met statistieken.</p></div>`
       - Remove the old user email display (that was a Phase 1 placeholder)

    2. **Create 5 placeholder pages** -- each follows the same pattern:
       - Server component (no 'use client', no data fetching needed)
       - Page title as `<h1>` with Dutch name
       - Short Dutch placeholder description
       - Each file exports a default function named descriptively

       **src/app/(client)/dashboard/inbox/page.tsx:**
       - Title: "Inbox"
       - Description: "Uw positieve leads verschijnen hier."

       **src/app/(client)/dashboard/verzonden/page.tsx:**
       - Title: "Verzonden"
       - Description: "Overzicht van alle verzonden e-mails."

       **src/app/(client)/dashboard/voorkeuren/page.tsx:**
       - Title: "Voorkeuren"
       - Description: "Beheer uw accountinstellingen."

       **src/app/(client)/dashboard/dnc/page.tsx:**
       - Title: "Do Not Contact"
       - Description: "Beheer uw uitsluitingslijst voor e-mailadressen en domeinen."

       **src/app/(client)/dashboard/afspraken/page.tsx:**
       - Title: "Afspraken"
       - Description: "Plan en bekijk uw afspraken."

    Each placeholder page should have consistent structure:
    ```tsx
    export default function XxxPage() {
      return (
        <div>
          <h1 className="text-2xl font-bold text-gray-900">[Title]</h1>
          <p className="mt-2 text-gray-600">[Description]</p>
        </div>
      )
    }
    ```
  </action>
  <verify>
    Run `npx tsc --noEmit` -- no type errors. Run `npm run build` -- all 6 routes build successfully. Verify each page file exists and exports a default component.
  </verify>
  <done>
    All 6 dashboard routes render without errors, each with Dutch title and placeholder text. Overzicht page includes branded accent element demonstrating theme integration.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete branded client dashboard shell with sidebar navigation, per-tenant theming, logo display, and 6 Dutch-labeled pages (Overzicht, Inbox, Verzonden, Voorkeuren, DNC, Afspraken).</what-built>
  <how-to-verify>
    1. Start dev server: `npm run dev`
    2. Log in as a test client user (use credentials from seed data)
    3. Verify sidebar appears on the left with:
       a. Client's company logo (or initial circle if no logo uploaded)
       b. Six navigation links with Dutch labels
       c. "Uitloggen" button at the bottom
    4. Click each navigation link -- verify:
       a. Page loads with correct Dutch title
       b. Active navigation item is highlighted with the client's brand color
       c. No layout shifts or errors
    5. Check the Overzicht page has a branded accent bar (colored left border) using the client's primary color
    6. Verify the brand color matches what was set for this client in the database
    7. Log out, then log in as a DIFFERENT test client (if available) -- verify their branding is different (different logo/color, no leakage from first client)
  </how-to-verify>
  <resume-signal>Type "approved" if branding, navigation, and layout look correct -- or describe any issues to fix.</resume-signal>
</task>

</tasks>

<verification>
1. `npm run build` succeeds with all 6 client routes
2. Each sidebar nav link maps to a real page that renders
3. Active state correctly highlights current page
4. Brand color visible in sidebar active state and Overzicht accent
5. Logo displays or falls back gracefully
6. All text is Dutch
7. No cross-client branding leakage
</verification>

<success_criteria>
- All 6 navigation links route to working pages
- Human verifies: branding looks correct, navigation works, Dutch labels present
- No build errors, no TypeScript errors
- Brand color isolation confirmed (different clients see different colors)
</success_criteria>

<output>
After completion, create `.planning/phases/03-client-dashboard-shell-branding/03-02-SUMMARY.md`
</output>
